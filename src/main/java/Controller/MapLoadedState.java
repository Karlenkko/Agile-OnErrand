package Controller;

import View.Window;

public class MapLoadedState implements State {
    // the only methods available here are the loadMap and loadRequest operations
    // which are defined by default in the interface
}
